{
  "name": "example-application",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "reverse-proxy"
  ],
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND,PROXY\" -c \"bgBlue,bgMagenta,bgGreen\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\" \"npm run dev --workspace=reverse-proxy\"",
    "build": "npm run build --workspaces",
    "start": "concurrently --names \"BACKEND,FRONTEND,PROXY\" -c \"bgBlue,bgMagenta,bgGreen\" \"npm run start --workspace=backend\" \"npm run start --workspace=frontend\" \"npm run start --workspace=reverse-proxy\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:proxy": "npm run dev --workspace=reverse-proxy",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:proxy": "npm run build --workspace=reverse-proxy"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
