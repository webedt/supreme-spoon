# Example environment variables for local development
# Copy this file to .env and fill in your values

# PostgreSQL database connection string
# Format: postgresql://username:password@hostname:port/database
# Leave commented out to use in-memory storage (temporary, resets on restart)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/simplesoup

# JWT secret key for authentication (optional, auto-generates if not set)
# Generate a secure random string: openssl rand -base64 32
# JWT_SECRET=your-secret-key-here

# Backend and Frontend URLs (usually don't need to change these)
# BACKEND_URL=http://localhost:3001
# FRONTEND_URL=http://localhost:5173

# Port configuration (usually don't need to change these)
# Backend runs on 3001, reverse proxy on 3000, frontend on 5173
